import 'package:flutter/material.dart';

class AppColors {
  // Fondo principal (El que quieres para todo)
  static const background = Color(0xFF030712); // gray-950
  
  // Colores de texto
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.grey; // gray-400
  static const border = Colors.white10;     // border-gray-800

  // Colores de la marca (Tus acentos)
  static const orange = Colors.orange;
  static const blue = Colors.blue;
  static const green = Colors.green;
  static const purple = Colors.purple;
}